jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v3
        with:
          version: 9

      - name: Install Foundry            # 👈 new
        uses: foundry-rs/foundry-toolchain@v1
        with:
          version: nightly               # or 'stable' / specific tag

      - name: Install deps
        run: pnpm install --frozen-lockfile

      - name: Lint
        run: pnpm turbo run lint

      - name: Contracts
        run: |
          cd contracts
          forge test

      - name: Build web
        run: |
          cd web
          pnpm build
